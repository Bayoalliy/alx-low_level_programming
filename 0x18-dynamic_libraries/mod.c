
#include "main.h"

/**
 * add - Entry point
 *
 * Return: addition of two integers.
 *
 * @a:  first parameter of integer type
 * @b:  parameters of integer type
 */
int mod(int a, int b)
{
	int added = a % b;

	return (added);
}
