#include <string.h>
/**
 * _strlen - returns the lenght of a string
 * Return: length of the string *s is pointing to
 *
 * @s : pointer to the variable str
 */
int _strlen(char *s)
{
	return (strlen(s));
}
